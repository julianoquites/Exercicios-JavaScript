function soma(n1, n2) { //Parâmetros opcionais -> dá pra por o n1=0 e o n2=0 
                        //para evitar erro, caso eu use só um parâmetro na chamada
    return n1 + n2
}
console.log(soma(2, 5))